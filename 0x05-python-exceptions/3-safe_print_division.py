#!/usr/bin/python3
def safe_print_division(a, b):
    try:
        result = a / b
        print(Ïnside result: {:.if}".format(result()
    except:
        result = None
        print(Ïnside result: {}".format(result))
    finally:
        return result
